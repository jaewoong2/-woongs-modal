import styled from '@emotion/styled'

export const Button = styled.button`
  border: none;
  background-color: transparent;
  padding: 0;
  width: fit-content;
  height: fit-content;
  cursor: pointer;
`
